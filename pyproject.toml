[project]
name = "paramrf"
version = "0.3.8"
description = "ParamRF: Parametric Microwave Circuit Modelling, Fitting and Sampling"
readme = "README.md"
authors = [
    { name = "Gary Allen", email = "gvcallen@gmail.com" }
]
dependencies = [
    "equinox",
    "jax",
    "jaxtyping",
    "h5py",
    "matplotlib",
    "numpy",
    "numpyro",
    "jaxopt",
    "jsonpickle",
    "scikit-rf",
    "scipy",
]

[project.optional-dependencies]
polychord = [
    "anesthetic",
    "fgivenx",
    "mpi4py",
    "pandas",
    "pypolychord @ git+https://github.com/PolyChord/PolyChordLite.git",
]
blackjax = [
    "blackjax @ git+https://github.com/handley-lab/blackjax@nested_sampling",
    "anesthetic",
]

[project.urls]
homepage = "https://github.com/gvcallen/paramrf"

[build-system]
requires = [
    "setuptools>=45",
    "wheel",
    "setuptools_scm[toml]>=6.2",
]
build-backend = 'setuptools.build_meta'

[tool.setuptools.packages.find]
include = ["pmrf"]