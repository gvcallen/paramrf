[project]
name = "paramrf"
version = "0.6.5"
description = "ParamRF: Parametric Circuit Modelling, Fitting and Sampling"
readme = "README.md"
authors = [
    { name = "Gary Allen", email = "gvcallen@gmail.com" }
]
dependencies = [
    "equinox",
    "jax",
    "jaxtyping",
    "h5py",
    "matplotlib",
    "numpy",
    "numpyro",
    "jaxopt",
    "jsonpickle",
    "scikit-rf",
    "scipy",
]

[project.optional-dependencies]
polychord = [
    "pypolychord @ git+https://github.com/PolyChord/PolyChordLite.git",
    "anesthetic",
    "fgivenx",
    "mpi4py",
    "pandas",
]
blackjax = [
    "blackjax @ git+https://github.com/handley-lab/blackjax@nested_sampling",
    "anesthetic",
    "tqdm",
]
docs = [
    "sphinx",
    "myst-parser",
    "ghp-import",
]

[project.urls]
homepage = "https://github.com/paramrf/paramrf"

[build-system]
requires = [
    "setuptools>=45",
    "wheel",
    "setuptools_scm[toml]>=6.2",
]
build-backend = 'setuptools.build_meta'

[tool.setuptools.packages.find]
include = ["pmrf"]